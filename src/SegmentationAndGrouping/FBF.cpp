#include "SegmentationAndGrouping.hpp"
#include "helpers/matrix.hpp"

namespace VisualAlgo::SegmentationAndGrouping
{
    FBF::FBF(float A, float B, float C, float D, float E, float S, float ALPHA, float BETA)
        : A(A), B(B), C(C), D(D), E(E), S(S), ALPHA(ALPHA), BETA(BETA)
    {
        
    }


}